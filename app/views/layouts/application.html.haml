!!! 5
%html
  %head
    %title Redmine

    %meta{name: "viewport", content: "width=device-width,initial-scale=1"}
    = csrf_meta_tags
    = csp_meta_tag

    %link{href: "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css",
          rel: "stylesheet"}

    = favicon_link_tag "redmine.svg"
    = stylesheet_link_tag "application", "data-turbo-track": "reload"
    = javascript_importmap_tags

  %body.d-grid{ style: "height: 100vh; grid-template-rows: auto 1fr auto;"}
    %header
      = render "layouts/navbar"
      = render "layouts/alert"

    %main
      = yield

    %footer.text-start.text-lg-start.bg-light
      .text-start.p-3
        © 2024 Daniel Sykora.

    %script{src: "https://code.iconify.design/iconify-icon/1.0.8/iconify-icon.min.js"}
    %script{src: "https://unpkg.com/htmx.org@1.9.10"}

    :javascript
      // $(document).ready(() => { htmx.process(document.body) })
      document.addEventListener("turbo:load", function() {
        htmx.process(document.body)
      });
